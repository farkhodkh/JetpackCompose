package com.example.jetpackcompose.ui.theme

import androidx.compose.ui.graphics.Color

internal val CongressBlue500 = Color(0xFF004D90)
internal val CongressBlue400 = Color(0xFF0793D6)
internal val TallPoppy500 = Color(0xFFBE3127)
internal val Silver = Color(0xFFB8B8B8)

private val GrayLight = Color(0xFFDFDFDF)
private val BorderGrayLight = Color(0xFFF8F8F8)
private val Gray = Color(0xFF8E8E93)
private val GrayDark = Color(0xFF525252)
private val SwitchDisabledBg = Color(0xFFE5E5E5)

private val FeedbackSuccess = Color(0xFF347C0E)
private val FeedbackSuccessLight = Color(0xFFDDEED5)
private val FeedbackInfo = Color(0xFF0793D6)
private val FeedbackError = Color(0xFFE4302B)
private val FeedbackWarning = Color(0xFFDE6B0D)
private val Highlight = Color(0xFFFFE439)
private val HighlightLighter = Color(0xFFFCF9AA)
private val DividerBackground = Color(0xFFF7F7F7)
private val FilterDividerBg = Color(0xFFDFDFDF)
private val SpecialBgPrimary = Color(0xFFDBEDFD)
private val SpecialBgSecondary = Color(0xFFEEF5FC)

private val Yellow = Color(0xFFFFE439)
private val YellowStar = Color(0xFFF4C540)
/**
 * More application colors.
 */
object MoreColors {
    /**
     * The disabled color.
     */
    val Disabled = Silver

    /**
     * The switch disabled color.
     */
    val SwitchDisabled = SwitchDisabledBg

    /**
     * Caption text color.
     */
    val CaptionText = GrayDark

    /**
     * Gray text color.
     */
    val TextGray = Gray

    /**
     * Highlight background color.
     */
    val BackgroundHighlight = Highlight

    /**
     * Highlight lighter background color.
     */
    val BackgroundHighlightLight = HighlightLighter

    /**
     * Divider color.
     */
    val Divider = DividerBackground

    /**
     * Filter divider color.
     */
    val FilterDivider = FilterDividerBg

    /**
     * Border color on filter chips.
     */
    val BorderGrayLight = GrayLight

    /**
     * Button border color on filter chips.
     */
    val ButtonBorderGrayLight = BorderGrayLight

    /**
     * Background color for sale items.
     */
    val SaleBg = Yellow

    /**
     * Rating background.
     */
    val RatingBg = YellowStar

}